function [D] = compute_drag(stateArray, Parameter)
%Computes the drag force acting on the rocket for a given state.
%   Uses the height of the rocket 
outputArg1 = inputArg1;
outputArg2 = inputArg2;
end

